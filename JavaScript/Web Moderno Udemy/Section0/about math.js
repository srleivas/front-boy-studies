// https://www.w3schools.com/js/js_math.asp 

// Same as round in C, just return the nearest int from X
const x = 9.7
Math.round(x)
console.log("\n")
console.log(("Round of 9.7 = "), x, ("\n"))

// Returns PI
console.log(("Math.PI ="), Math.PI, ("\n"))

// Returns a to the power of b
const a = 3
const b = 2
console.log(("3 to the power of 2 ="), Math.pow(b, a), ("\n"))

// Returns the square Root of n

const n = 12
console.log("Square of 12 =", Math.sqrt(n), ("\n"))

// As you can see, it's really useful. No, i'll not write the whole page here cmon.

// Will not return 1, because sometimes javascript's cool like a banana
text = 'Text'
console.log(text.charAt(5))